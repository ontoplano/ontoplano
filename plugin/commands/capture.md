---
description: Put something into ontoplano — a to-do, a note, an idea, an item
---

Put what follows into ontoplano, in the person's own words.

$ARGUMENTS

Pick where it belongs rather than asking: a thing to do is `add_task`, a
thought is `add_idea`, something to buy is `add_inventory_item`, something
written is `write_entry`. If it genuinely could be two of those, ask. Say where
it landed.
