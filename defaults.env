# Every tunable default of this repo's tooling, in one file — the place to
# look when a value is not set anywhere else. The Makefile includes it and
# scripts may source it; static KEY=value only, so make, a shell and systemd
# all read the same thing. A deployment overrides these in local.mk (which
# loads after) or on the command line — never by editing this file.

# The port the app serves, dev and production alike.
APP_PORT=1493

# Where `make android-share` serves the built APK on the LAN.
APK_PORT=8088

# The Android signing key and identity. The package name is permanent once
# published; the alias and keystore path only change if the key does.
ANDROID_KEYSTORE=android-twa/android.keystore
ANDROID_KEY_ALIAS=ontoplano
ANDROID_PACKAGE_NAME=app.ontoplano.twa

# Hide the bands that name the instance — "Demo version", "Staging" — because
# they are exactly what you do not want across the bottom of a video. It hides
# the bands and nothing else: a demo still expires accounts and still limits
# how many there are. Never set this on a box the public can reach.
ONTOPLANO_RECORDING=false

# An instance running on the device it is used from — the phone build.
#
# Off by default, because the default here is somebody's own server: a machine
# that stays on and answers the door. An isolated instance cannot be reached from
# outside and is not awake when the app is closed, so the features that depend
# on either say so rather than failing quietly. See src/lib/capabilities.ts.
ONTOPLANO_ISOLATED=false
