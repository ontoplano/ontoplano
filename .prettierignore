# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/
/drizzle/

# Anything else a particular checkout wants left alone — another repository
# cloned inside this directory, for instance — goes in `.ignore.local`, which
# `yarn lint` passes as a third ignore file and which is itself untracked. That
# is the point: a published ignore file describes whatever it names.

# The Android project the stores build. Generated by Bubblewrap and committed
# so F-Droid can build a tag without a network; restyling its JSON would be
# undone by the next `make android-project` and mean nothing in between.
android/
