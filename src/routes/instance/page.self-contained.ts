/**
 * Nothing to load — and that is why this file exists.
 *
 * The device's routing table is built from the page twins beside each route,
 * so a page with no twin is a page the device cannot answer for, however
 * little it needs. This one needs nothing at all: where your ontoplano lives
 * is a choice made in the browser and kept there. The empty twin is what says
 * "yes, this screen runs here" — without it, the only way off a phone-only
 * instance answers "this screen needs a server", which would be funny if it
 * were not the exit.
 */
export {};
